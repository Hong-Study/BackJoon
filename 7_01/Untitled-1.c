#include<stdio.h>
#include<stdlib.h>

int main(){
    int n;
    int 

}
/*
int main(){
    int n;
    int *num;
    int total;
    int com[4001];
    int mid;
    int min = 4001;
    int max = 0;
    scanf("%d",&n);
    num = (int*)malloc(sizeof(int)*n);
    for(int i=0;i<n;i++){
        scanf("%d",&num[i]);
        if(num[i] > max)
            max = num[i];
        else if(num[i] < min)
            min = num[i];
        total += num[i];
        com[num[i]]++;
    }
    return 0;
}
*/